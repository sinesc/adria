
var Node = require('./base/node');

module ThrowStatement = proto (Node) {

    toSourceNode: func() {

        var result = this.csn('throw ');
        result.add(this.get('exception').toSourceNode());
        result.add(';' + this.nl());

        return result;
    },
};
