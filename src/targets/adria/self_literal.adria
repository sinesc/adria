
var AdriaNode = require('./adria_node');
var FunctionLiteral = require('./function_literal');

module SelfLiteral = proto (AdriaNode) {

    toSourceNode: func() {

        var host = this.findProto(FunctionLiteral);
        return this.csn(host.getSelfLookup());
    }
};
