
var Node = require('./base/node');

module ConstLiteral = proto (Node) {

    toSourceNode: func() {

        var stringNode = this.get('string');

        if (stringNode.isNode()) {
            return this.csn(stringNode.value);
        } else {
            return this.csn(this.get('numeric').value);
        }
    },
};