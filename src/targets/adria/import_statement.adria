
var Node = require('./base/node');

module ImportStatement = proto (Node) {

    toSourceNode: func() {

        var scope = this.findScope();

        this.eachKey('item', func(node) {
            scope.addImplicit(node.value);
        });

        return this.csn();
    },
};
