
var Node = require('./base/node');
var FunctionLiteral = require('./function_literal');

module ParentLiteral = proto (Node) {

    toSourceNode: func() {

        var host = this.findProto(FunctionLiteral);
        return this.csn(host.getParentLookup());
    }
};
